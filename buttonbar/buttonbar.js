// The following code can be used to insert a sample progress bar through the ButtonBar plugin:
// the associated icon is also included

	case 'progress':
	   $(TextArea).val($(TextArea).val() + " Item: [progress 12]\n");
	   break;